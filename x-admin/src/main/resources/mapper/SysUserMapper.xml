<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.SysUserMapper">

    <select id="selectAllocatedList" resultType="com.example.admin.domain.entity.SysUser">
        select distinct u.* from sys_user u
        left join sys_user_role ur on u.user_id = ur.user_id
        where ur.role_id = #{user.roleId}
        <if test="user.username != null and user.username != ''">
            and u.username like concat('%', #{user.username}, '%')
        </if>
        <if test="user.phone != null and user.phone != ''">
            and u.phone like concat('%', #{user.phone}, '%')
        </if>
        <if test="user.status != null">
            and u.status = #{user.status}
        </if>
    </select>

    <select id="selectUnallocatedList" resultType="com.example.admin.domain.entity.SysUser">
        select distinct u.* from sys_user u
        left join sys_user_role ur on u.user_id = ur.user_id and ur.role_id = #{user.roleId}
        where ur.role_id is null
        <if test="user.username != null and user.username != ''">
            and u.username like concat('%', #{user.username}, '%')
        </if>
        <if test="user.phone != null and user.phone != ''">
            and u.phone like concat('%', #{user.phone}, '%')
        </if>
        <if test="user.status != null">
            and u.status = #{user.status}
        </if>
    </select>

</mapper> 